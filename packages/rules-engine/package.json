{
  "name": "@abrid/rules-engine",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/index.test.js || (npm run build && node --test dist/test/index.test.js)"
  },
  "dependencies": {
    "@abrid/types": "0.1.0",
    "tz-offset": "^0.0.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}